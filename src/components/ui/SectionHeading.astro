---
import { visitorLangFromPath } from "../../utils/i18n-lang";
import { translate } from "../../utils/translations";

interface Props {
  text: string;
  i18nKey?: string;
  className?: string;
  /** Por defecto `h2`; usa `h1` cuando no hay otro título principal en la página. */
  as?: 'h1' | 'h2';
}

const { text, i18nKey, className = '', as: HeadingTag = 'h2' } = Astro.props;
const lang = visitorLangFromPath(Astro.url.pathname);
const resolvedText = i18nKey ? translate(i18nKey, lang, text) : text;
---

<HeadingTag class={`df-section-title ${className}`.trim()}>
  {i18nKey ? <span data-i18n={i18nKey}>{resolvedText}</span> : resolvedText}
</HeadingTag>
